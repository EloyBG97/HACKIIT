Dado el fichero de texto CypherText, podemos comprobar que el fichero está formado por números en decimal, octal (empizan por 0), 
hexadecimal (solo los que contienen digitos en [a-f]) y binario.

Para descifrar el texto, adjunto un programa que pasa cada numero al caracter ASCII correspondiente.
